import { HoraFormatadaPipe } from './hora-formatada-pipe';

describe('HoraFormatadaPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraFormatadaPipe();
    expect(pipe).toBeTruthy();
  });
});
